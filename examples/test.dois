module MyModule has
  type Result is
    Err | Nil
  end

  type Err has
    message : Int
  end

  fn foo$() : Int =>
    5
  end

  # fn add$(x : Int, y : Int) : Int =>
  #   x + y
  # end

  proc main() do
    let x : Int = 5;
    let y : Int = 10;
    let sum = x + y;
  end
end

  